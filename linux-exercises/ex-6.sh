#!/bin/bash

#* Author : Dillon Juriansz

#* 6. Network-related Commands:
#* - Use the `ping` command to check the connectivity to a specific host.
#* - Use the `nslookup` command to find the IP address of a domain name.
#* - Use the `netstat` command to display open network ports on your system.

ping opera.com
nslookup opera.com
netstat -tuln
